import java.util.*;
class Quicksort {
    static List<Integer> liste = new ArrayList<>(
            List.of(1,5,2,4,0,3)
    );
    public static void main(String[] args){
        // System.out.println(quicksort(liste).get(0));
    }

    public static List<Integer> quicksort(List<Integer> liste){
        List<Integer> sorted_list = new ArrayList<>();

        int left = sorted_list.get(0);
        int right = sorted_list.get(sorted_list.size()-1);
        int pivot = left;

        return sorted_list;
    }
}
