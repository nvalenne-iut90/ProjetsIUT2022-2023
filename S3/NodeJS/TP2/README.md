.env (pour mettre les vaariables comme le port)\
```shell
touch .env
```

ajouter dans packege.json
```json
    "start":"nodemon server.js"
```
dans le terminal : npm start

pour tester : http://localhost:3000

